{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "build g++",
      "command": "/usr/bin/make",
      "args": ["build"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build"
    }
  ]
}
